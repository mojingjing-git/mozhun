# 2026-09-05 工作日志

## 新时代校对大师：git 初始化 + 全量 Python 后端审计

### git 初始化
- `git init` + 新建 `.gitignore`（排除 .venv/build/dist/logs/*.tmp/nul/`.baseline-v2.0`/一次性验证输出）
- 初始基线提交 `55bb965`（v4.1.8，64 文件，21435 insertions）
- git 身份 local 配置为 wrz20 / wrz20@localhost

### 全量代码审计（web/ 前端除外）
- 方法：parallel-code-audit skill — 6 并行只读子代理按架构分片（web_backend / processor+context_builder / utils+logger / templates+style_profiles / 入口构建 / 测试套件）+ 1 独立验证器逐条复检
- 规模：约 5,100 行产品代码 + 约 3,900 行测试，共 43 条发现
- **验证结果：38 CONFIRMED / 3 PARTIAL / 1 NOT FOUND(B8 代理对误解) / 1 CANNOT_VERIFY**
- 严重度分布：12 HIGH / 18 MEDIUM / 12 LOW
- 完整报告：`audit_reports/07_full_python_audit_20260905.md`

### 最高优先 12 个 HIGH（详见报告）
1. api_key 混淆串往返污染 → "保存一次设置后全线 401"（utils.py:181 + web_backend.py:100-116 + app.js:221）
2. start_proofread 无并发重入保护 → 旧任务回调 KeyError/双线程同引擎
3. RateLimit 耗尽路径不更新 stats（processor.py:481-501）
4. cancel 落在排队/重试间隙的文件不留痕迹 → UI 卡"排队中"（processor.py:289-294/317-326）
5. 断点 hash 漏 chunking_preset/context_max_* 字段（utils.py:50-53）
6. expected_hash 生产零调用 + 空 hash 旧断点永不失效（processor.py:165-171）
7. check_exe.py 正斜杠探测 Windows exe 恒误报（实证 b"web/" 0 次）
8. build.spec datas 漏 style_profiles/builtin/*.json → 打包后 3 套内置文风丢失
9. 预置 profile 对去AI味 step2/3 静默无效（get_builtin 零调用，v4.1.7 功能半实现）
10. **测试污染真实 ~/.proofreader**（已造成现实损害！）
11. v4.1.5 renderState mock 过度 + 3 个纯恒真用例
12. custom_prompt 残留无条件覆盖任意校对模式

### ⚠️ 现实损害（需用户处理）
- 测试套件审计子代理跑测试后，`~/.proofreader/last_config.json` 被覆盖为测试值（api_base=http://test、api_key 被清空、594 个孤儿断点）
- 用户需重新填 API 配置；孤儿断点可删（等待用户确认清理）

### 待办（用户确认后）
- 按 P0/P1/P2 分批修复；修复后重打包需验证 build.spec 修复 + check_exe 修复
